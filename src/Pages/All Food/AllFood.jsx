// import { } from 'react';
// import { use } from 'react';
// import { use } from 'react';
import FoodCard from './FoodCard';

const AllFood = () => {

  // const foods = use(foodPromise)

  // const foods = use(foodPromise);
  // const foods = ()=>{fetch('http://localhost:3000/menu').then(res =>res.json())}

  return (
    <div>
      <h1 className='text-2xl font-bold text-center my-10'>Menu Section</h1>
      {/* {foods.length} */}
      <div>
        {/* {
          foods.map(food => <FoodCard   food={food}></FoodCard>)
        } */}
      </div>

    </div>
  );
};

export default AllFood;